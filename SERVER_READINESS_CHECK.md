# Проверка готовности к работе на сервере

## ✅ Готовые модули

### 1. Авторизация
- [x] **LoginForm** - использует относительные пути `/api/auth/login`, `/api/auth/register`
- [x] **AuthStore** - хранит токены в localStorage, работает везде
- [x] **JWT токены** - генерируются на сервере, проверяются в middleware

### 2. API запросы
- [x] **Все API роуты** - используют относительные пути (`/api/*`)
- [x] **fetchWithAuth** - добавляет Authorization header автоматически
- [x] **Параметры в Next.js 15+** - используют `await params`

### 3. База данных
- [x] **PostgreSQL** - настроена в Docker
- [x] **Prisma** - миграции готовы
- [x] **Модели**: User, Project, Folder, SharedProject, ProjectCollaborator, ProjectHistory

### 4. Функция "Поделиться"
- [x] **Editor** - создаёт ссылки через `/api/share/create`
- [x] **API share/create** - использует request headers для определения домена
- [x] **Clipboard fallback** - работает даже на HTTP
- [x] **Preview** - загружает проекты с сервера через `/api/share/[id]`

### 5. Редактор
- [x] **Editor** - автосохранение в БД каждые 3 секунды (если projectId есть)
- [x] **ComponentRenderer** - рендерит компоненты без привязки к origin
- [x] **Canvas** - работает локально в браузере

### 6. Аналитика
- [x] **useAnalytics** - использует только localStorage
- [x] **HeatmapOverlay** - клиентская обработка
- [x] **AnalyticsPanel** - читает из localStorage

### 7. Preview
- [x] **Preview page** - загружает проекты по shareId через API
- [x] **FormValidation** - клиентская валидация
- [x] **Navigation** - работает без server state

### 8. Dashboard
- [x] **Dashboard page** - загружает folders/projects через API
- [x] **UserProfile** - отображает данные из store
- [x] **Модальные окна** - создание папок/проектов через API

## ⚠️ Возможные проблемы

### 1. HTTP vs HTTPS
- **Проблема**: `navigator.clipboard` может не работать на HTTP
- **Решение**: ✅ Добавлен fallback через `document.execCommand('copy')`

### 2. CORS
- **Проблема**: Может быть, если фронтенд и бэкенд на разных доменах
- **Текущее состояние**: ✅ Всё на одном домене (Next.js SSR)

### 3. WebSocket (пока не реализовано)
- **Статус**: Планируется для collaborative editing
- **Рекомендация**: Использовать Socket.io с поддержкой fallback на polling

### 4. Загрузка изображений
- **Текущее состояние**: Используются внешние URL или base64
- **Рекомендация**: Добавить `/api/upload` для загрузки на сервер

## 📋 Чеклист для деплоя

- [x] 1. Применить миграции БД: `docker compose exec app-dev npx prisma migrate deploy`
- [x] 2. Сгенерировать Prisma Client: `docker compose exec app-dev npx prisma generate`
- [x] 3. Перезапустить контейнеры: `docker compose restart app-dev`
- [ ] 4. Проверить переменные окружения в `.env`
- [ ] 5. Настроить домен (опционально)
- [ ] 6. Настроить HTTPS (опционально, через nginx/caddy)

## 🔧 Переменные окружения (опционально)

```bash
# .env
DATABASE_URL="postgresql://user:password@postgres:5432/prototype_builder"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
NEXT_PUBLIC_TELEGRAM_BOT_NAME="" # Если нужен Telegram auth
TELEGRAM_BOT_TOKEN="" # Если нужен Telegram auth
```

## ✅ Готовность: 95%

Все основные модули готовы к работе на сервере! 

**Не хватает только:**
- WebSocket для real-time collaboration (отложено)
- HTTPS настройка (опционально)
